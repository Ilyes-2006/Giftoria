# Giftoria Backend

A lightweight Express backend for the Giftoria ecommerce project.

## Setup

1. Open a terminal in `back/`
2. Run `npm install`
3. Start the server with `npm run dev` or `npm start`

## API Endpoints

- `GET /api/products` — list products
- `GET /api/products/:id` — fetch a single product
